generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://postgres:password@localhost:5432/seo_agent_db?schema=public"
}

model User {
  id    String @id @default(uuid())
  email String @unique
}
